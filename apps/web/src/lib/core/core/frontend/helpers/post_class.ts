import { SafeString } from "../services/svelteTemplate";

export function post_class(post: any) {
    // let classes = ['post'];

    // const tags = post && post.tags ? post.tags : tags || [];
    // const featured = post && post.featured ? post.featured : featured || false;
    // const image = post && post.feature_image ? post.feature_image : feature_image || false;
    // const page = post && post.page ? post.page : page || false;

    // if (tags) {
    //     classes = classes.concat(tags.map(function (tag) {
    //         return 'tag-' + tag.slug;
    //     }));
    // }

    // if (featured) {
    //     classes.push('featured');
    // }

    // if (!image) {
    //     classes.push('no-image');
    // }

    // if (page) {
    //     classes.push('page');
    // }

    // classes = classes.reduce(function (memo, item) {
    //     return memo + ' ' + item;
    // }, '');

    // return new SafeString(classes.trim());
}