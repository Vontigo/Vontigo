interface HashOptions {
    expires: number;
    email: string;
    dbHash: string;
    password: string;
}

interface OriginalValues {
    expires: number;
    email: string;
    dbHash: string;
    password: string;
}