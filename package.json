{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "web": "turbo run dev --filter=web",
    "v2": "turbo run dev --filter=sveltekit-latest",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^3.4.2",
    "prettier-plugin-svelte": "^3.3.2",
    "turbo": "^2.3.3"
  },
  "browser": {
    "crypto": false
  },
  "packageManager": "pnpm@10.14.0",
  "dependencies": {
    "better-sqlite3": "^12.2.0"
  }
}
